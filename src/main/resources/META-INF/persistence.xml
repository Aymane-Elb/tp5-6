<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
                                 http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">

    <persistence-unit name="UP_CATALOGUE" transaction-type="JTA">
        <!-- Datasource JNDI dans WildFly -->
        <jta-data-source>java:/MySqlDS</jta-data-source>

        <!-- Entités -->
        <class>ma.ensak.entities.Produit</class>

        <properties>
            <!-- Création / mise à jour auto du schéma -->
            <property name="jakarta.persistence.schema-generation.database.action" value="update"/>

            <!-- Forcer le schéma / base -->
            <property name="hibernate.default_catalog" value="db_catalogue"/>

            <!-- Dialecte MySQL -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.MySQLDialect"/>

            <!-- Log SQL pour debug -->
            <property name="hibernate.show_sql" value="true"/>
        </properties>
    </persistence-unit>
</persistence>

